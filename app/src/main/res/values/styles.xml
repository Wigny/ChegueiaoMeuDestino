<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:timePickerDialogTheme">@style/AppTheme.TimePickerDialog</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.Launcher" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.TimePickerDialog" parent="android:Theme.Material.Light.Dialog">
        <item name="android:colorAccent">@color/colorPrimaryDark</item>
    </style>

    <!-- DARK -->

    <style name="AppTheme.Dark" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorPrimary">@color/darkColorPrimary</item>
        <item name="colorPrimaryDark">@color/darkColorPrimaryDark</item>
        <item name="colorAccent">@color/darkColorAccent</item>
        <item name="android:alertDialogTheme">@style/AppTheme.Dark.AlertDialog</item>
        <item name="android:timePickerDialogTheme">@style/AppTheme.Dark.TimePickerDialog</item>
    </style>

    <style name="AppTheme.Dark.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.Dark.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.Dark.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark" />

    <style name="AppTheme.Dark.AlertDialog" parent="android:Theme.Material.Dialog.Alert">
        <item name="android:colorAccent">@color/darkColorAccent</item>
        <item name="android:colorBackground">@color/darkColorPrimary</item>
    </style>

    <style name="AppTheme.Dark.TimePickerDialog" parent="android:Theme.Material.Light.Dialog">
        <item name="android:colorAccent">@android:color/background_dark</item>
        <!--<item name="android:headerBackground">@color/colorPrimaryDark</item>-->
    </style>
</resources>
